import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TrigentAI - AI-Powered Business Intelligence Platform',
  description: 'Transform your business with AI-powered insights, automation, and growth strategies.',
  keywords: ['AI', 'Business Intelligence', 'Automation', 'Growth Analytics', 'Dashboard'],
  authors: [{ name: 'TrigentAI Team' }],
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}